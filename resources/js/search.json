[[{"l":"Peretum","p":["A lightweight, high-performance HTTP/HTTPS reverse proxy with transparent disk-based response caching, host + location routing, a plugin system, HTTP/3 (QUIC), gRPC passthrough and a Web Application Firewall."]},{"l":"Features","p":["Transparent disk cache — serves subsequent requests from disk with X-Cache: HIT; LRU eviction, background sweep, atomic writes and request coalescing.","Routing — prefix, exact and regex location matching, per-host targets with a default fallback handler.","Load balancing — round-robin, weighted, Maglev consistent hashing and least-connections, with passive health tracking.","nginx-style listeners — bind any number of frontends with ssl, h2c and h3/quic flags.","TLS — global and per-target certificates, automatic self-signed certificate generation.","HTTP/3 (QUIC) — served on the listen port by default; HTTP/1.1, HTTP/2 and HTTP/3 all work out of the box.","gRPC — pure bidirectional streaming passthrough (h2c + ALPN h2), never cached or buffered.","Plugins — compression, CSS/JS/image optimization, CORS, request and response headers, path rewrite, JSON access/error logs, Prometheus metrics, branded error pages and a Web Application Firewall.","WAF — per-location signature rules (Aho–Corasick driven contains), operates on geo/IP fields as ordinary rule conditions.","Hot reload — SIGHUP reloads configs and rotates log outputs; graceful 30 s shutdown on SIGINT/SIGTERM."]},{"l":"Quick start","p":["Serves on :8081 by default using config.yaml and config.d/. See the Quickstart for the details."]},{"i":"quick-start-code-1","c":["go run ."]},{"l":"Documentation","p":["Adding a plugin","CLI — peretum, peretum -t, peretum -r and flags","Compression","Configuration","Configuration overview","Contributing","Core subsystems","CORS","Development","Development index","Disk cache","Error pages","Getting started","Global configuration — config.yaml","gRPC proxying","Headers","HTTP/3 (QUIC)","JSON logs","Listeners, TLS & HTTP/3","Optimizer","Plugin index","Plugins","Project layout","Prometheus metrics","Quickstart — build, run, first config, test server","Rewrite","Routing & load balancing","Targets & locations — config.d/*.yaml","Testing","Web Application Firewall"]}],[{"l":"Quickstart"},{"l":"Prerequisites","p":["Go 1.26.2+ (the module lives at github.com/cinvat/peretum)."]},{"l":"Build and run","p":["This starts the proxy using config.yaml and the target files in config.d/ and writes a pid file (peretum.pid) for --reload.","To run without building:"]},{"i":"build-and-run-code-1","c":["go build -o peretum .\n./peretum"]},{"i":"build-and-run-code-2","c":["go run ."]},{"l":"Verify the installation","p":["-t validates config.yaml and every config.d/*.yaml without starting the server: size/duration fields, upstream URLs, listener directives and per-location cache_ttls."]},{"i":"verify-the-installation-code-1","c":["./peretum -t\n# config OK"]},{"l":"First request","p":["The default config.yaml listens on :8080 (plain), :443 and :8443 (TLS + HTTP/3). A self-signed certificate is generated automatically when TLS or HTTP/3 is requested but no certificate is configured, so:","With the default config.d/ files you will reach an upstream target; if the upstream is not running you get a 502 Bad Gateway."]},{"i":"first-request-code-1","c":["curl -k https://localhost:8443/"]},{"l":"Test server","p":["cmd/testserver provides an upstream for manual and integration testing:","Endpoints:","/style.css — repeated CSS block (~5.6 KB) for compression testing","/script.js — repeated JS block (~5.1 KB) for compression/optimizer","/image.png — minimal PNG header","anything else — a plain-text line echoing the request path"]},{"i":"test-server-code-1","c":["go run ./cmd/testserver\n# serves on 127.0.0.1:8082"]},{"l":"Ready to configure?","p":["See:","Global configuration for config.yaml","Targets & locations for config.d/*.yaml","CLI for reload and config checking"]}],[{"l":"Command line","p":["peretum is a single command (cobra-based):","The default command runs the proxy with a graceful shutdown: SIGINT / SIGTERM trigger a 30 second graceful drain of in-flight requests."]},{"i":"command-line-code-1","c":["peretum                  # run the proxy (defaults below)\nperetum -t               # check configuration syntax and exit ('config OK')\nperetum -r               # reload the running proxy (SIGHUP to the pid file)"]},{"l":"Flags","p":["--config path","--pid-file path","--targets dir","-r, --reload","-t, --test","config.d","config.yaml","Default","Description","Directory of per-target configuration files.","false","Flag","Path to the proxy configuration file.","peretum.pid","Pid file written at startup and read by --reload.","Read the running proxy's pid file and send it SIGHUP for a hot config reload.","Validate config.yaml and every config.d/*.yaml without starting the server; prints config OK."]},{"l":"--test (config check)","p":["checkConfig validates, without binding ports:","max_cache_size, max_cache_age, max_response_body_size size/duration formats;","max_write_workers is -1 (unlimited) or greater;","every listeners: directive (syntax, flags, duplicate flags, conflicting ssl + h2c);","every target's upstream URLs;","every per-location cache_ttl duration."]},{"l":"--reload (hot reload)","p":["Sending SIGHUP (via peretum -r or kill -HUP pid) to a running proxy:","reloads and re-validates config.yaml and config.d/;","rebuilds the host/location router and re-applies TLS certificates (GetConfigForClient for dynamic per-target certs);","rotates log outputs on plugins that implement Reopen() (the JSON-log plugin), which supports logrotate-style setups.","Plugin behavior changes (e.g. rule lists, levels) require a full restart: Init is not re-run on reload."]},{"l":"Exit codes","p":["A non-zero exit code is returned on any startup or reload failure; syntax errors from -t exit non-zero as well."]}],[{"l":"Configuration","p":["Peretum uses two kinds of YAML files:","config.yaml — global proxy settings: listeners, disk cache, metrics, JSON logging, error pages, the shared WAF GeoLite directory, and tunables.","config.d/*.yaml — one file per target: host key, upstreams, load balancing, per-location behavior (cache, plugins, proxy options)."]},{"i":"configuration-code-1","c":["./peretum -t                 # validate everything before starting"]},{"l":"Loading order and precedence","p":["The global config is loaded and validated.","Every config.d/*.yaml target is loaded (sorted); the target named listen-less entries become fallback/default targets.","listeners: (global) takes precedence over the legacy listen shorthand.","Per-location plugin blocks (compression, optimize, rewrite, cors, headers) are global and last-wins — the plugin config is overwritten for each configured location, so one instance applies proxy-wide (see Plugins for details and the WAF exception)."]},{"l":"Pages","p":["Global configuration (config.yaml)","Targets & locations (config.d/*.yaml)","Listeners, TLS & HTTP/3","Reference tables on each page come directly from the configuration types in internal/config/config.go."]}],[{"l":"Global configuration (config.yaml)"},{"i":"global-configuration-configyaml-code-1","c":["# nginx-style multi-listener frontends (takes precedence over `listen`).\n# Each entry: address[:port] [flags]; flags: ssl, h2c, h2/http2, h3/http3/quic.\n# A bare host or port defaults to 443 for ssl/h3 listeners, 80 otherwise.\nlisteners:\n  - \":8080\"            # plaintext; HTTP/1.1 + h2c\n  - \":443 ssl h2\"      # HTTPS; HTTP/1.1 + HTTP/2 via ALPN\n  - \":443 h3\"          # HTTP/3 (UDP) on the same port (implies TLS)\n  - \":8443 ssl h3\"     # HTTPS + HTTP/3 on a dedicated port (UDP + TCP)\n\n# The legacy shorthand is equivalent to a single plaintext listener\n# (TLS/HTTP/3 implied when certs are configured; http3_addr pins QUIC):\n# listen: \":8081\"\n# http3_addr: \":8443\"\n\ncache_dir: \"./cache\"\nmax_cache_size: \"500MB\"          # size string; 0 = unlimited\nmax_cache_age: \"24h\"             # Go duration; 0 = no expiration\nmax_write_workers: 8             # -1 = unlimited; 0 = default (8)\nmax_response_body_size: \"50MB\"   # -1 = unlimited; empty = default (50MB)\n\n# tls_cert_file: \"./certs/server.crt\"   # global HTTPS certificate\n# tls_key_file: \"./certs/server.key\"\n\nmetrics_addr: \":9090\"            # enables the prometheus_exporter plugin\n\njson_log:\n  enabled: true\n  access_log: \"./logs/access.jsonl\"\n  error_log: \"./logs/error.jsonl\"\n  stdout: true\n\nerror_page:\n  enabled: true\n  statuses: [403, 404, 500, 502, 503, 504]   # default: [404, 500, 502, 503, 504]\n\nwaf:\n  enabled: true\n  geolite_dir: \"./plugins/waf/geolite\"       # shared by every location's WAF"]},{"l":"Reference","p":["[]string","cache_dir","Description","duration","Enables prometheus_exporter on this address.","Entry lifetime. 0 = no time-based expiration.","error_page","Global TLS certificate.","Global WAF settings: enabled, geolite_dir (see WAF).","http3_addr","int","json_log","Key","Largest response body buffered before proxying. -1 = unlimited.","Legacy HTTP/3 (QUIC/UDP) address; implied when TLS is active.","Legacy single-address shorthand; ignored while listeners is set.","listen","listeners","Max concurrent cache writes. -1 = unlimited, 0 → 8, 0 = cap. When the pool is full, cache writes for a request are skipped (write pool full).","max_cache_age","max_cache_size","max_response_body_size","max_write_workers","metrics_addr","nginx-style frontends (see Listeners).","object","Root directory for the disk cache (created if missing).","See Error pages.","See JSON logs.","size string","string","tls_cert_file / tls_key_file","Total cache bytes; suffixes B/KB/MB/GB (binary, float prefixes allowed). 0 = unlimited.","Type","waf"]},{"l":"Size strings","p":["max_cache_size and max_response_body_size accept binary suffixes with optional fractional prefixes, e.g. 500MB, 1.5GB, 1024KIB. The longer/special suffixes (GIB, MIB, KIB) are checked first so B never swallows MB."]},{"l":"Durations","p":["max_cache_age and per-location cache_ttl use Go time.ParseDuration syntax: 30s, 5m, 1h30m, 24h."]}],[{"l":"Targets (config.d/*.yaml)","p":["One YAML file per target:"]},{"i":"targets-configdyaml-code-1","c":["name: \"api\"                        # host key, or use `listen:` with a host\nlisten: \"api.example.com\"          # host key (a bare \":port\" binds nothing)\nlb_algorithm: \"maglev\"\nupstreams:\n  - url: \"https://api1.example.com\"\n    weight: 3\n  - url: \"https://api2.example.com\"\n    weight: 2\ntls:                             # optional per-target certificate\n  cert_file: \"./certs/api.example.com.crt\"\n  key_file: \"./certs/api.example.com.key\"\nlocations:\n  - path: \"/api/\"\n    match_type: \"prefix\"\n    cache: true\n    cache_ttl: \"1h\"\n    ..."]},{"l":"Target fields","p":["[]object","cert_file, key_file — per-target certificate presented via SNI.","Description","Host key for this target. A host-only value (e.g. api.example.com) becomes the router key; :port-only values do not create a host key.","Key","lb_algorithm","listen","locations","name","object","Ordered location list; first match wins (see priorities below).","See load balancing.","string","Target name; also the router host key unless listen overrides it.","Targets with no host key act as the default/fallback handler; the router also falls back to a target whose first matching location is path: /.","tls","Type","upstreams","url (required), weight (= 0 treated as 1), health_check."]},{"l":"Locations","p":["A location is matched by path (first match wins, ordered by priority):","match_type","Priority","Notes","exact","1000","path == request path","regex","500","full Go regex against the path","prefix (default)","length of trimmed path","longest prefix wins; / matches everything"]},{"l":"Location fields","p":["bool","cache","cache_ttl","cors, headers, rewrite, optimize, compression, waf","Description","duration","Enable the disk cache for this location.","Key","Match pattern.","match_type","object","Overrides max_cache_age for entries of this location.","path","prefix (default), exact, regex.","proxy","See Plugins.","string","Type","websocket, pass_host_header, upstream (override), timeouts, buffering, buffer_size."]},{"i":"location-fields-code-1","c":["- path: \"/api/\"\n  match_type: \"prefix\"\n  cache: true\n  cache_ttl: \"1h\"            # per-entry TTL, overrides global max_cache_age\n  proxy:\n    pass_host_header: true\n    websocket: true\n    connect_timeout: \"5s\"\n    read_timeout: \"30s\"\n    send_timeout: \"30s\"\n    upstream: \"http://special-backend:8080\"   # per-location upstream override\n    buffering: true\n    buffer_size: \"64k\"\n  cors: {...}                # plugin blocks below\n  headers: {...}\n  rewrite: {...}\n  optimize: {...}\n  compression: {...}\n  waf: {...}"]},{"l":"Load-balancing algorithms","p":["Value","Behavior","round_robin / rr / (empty)","Equal distribution (default).","weighted_rr / wrr","Respects weight (smooth weights).","maglev","Maglev consistent hashing over a 65537-slot table, keyed by CRC32(path + rawQuery) — minimal reshuffle across upstream changes.","least_conn / least_connections","Routes to the upstream with the fewest active requests."]},{"l":"Health tracking","p":["All algorithms skip unhealthy upstreams. Upstreams are marked unhealthy on transport errors and healthy again on successful reads; when every upstream is unhealthy the least-recently-checked one is probed."]}],[{"l":"Listeners","p":["A quic-only directive like :443 h3 binds UDP only; combine with ssl (same line or a separate :443 ssl line) to also open the TCP listener on that port.","A wildcard address ( or :) binds all interfaces.","Accepted for nginx parity; HTTP/2 is always negotiable (ALPN over TLS, h2c on plaintext).","Duplicate flags on one directive are a configuration error.","Flag","h2 / http2","h2c","h2c and ssl on the same directive is a configuration error.","h3 / http3 / quic","Meaning","Peretum can bind any number of frontends with the nginx-style listeners: directive. Each entry is address[:port] [flags]:","Rules:","Serve HTTP/3 over QUIC/UDP (implies TLS).","Serve TLS on the TCP listener (HTTP/1.1 + HTTP/2 via ALPN).","ssl","The legacy shorthand is equivalent to a single plaintext listener:","Unencrypted HTTP/2 (conflicts with ssl).","When no port is given, it defaults to 443 for ssl/h3 listeners and 80 otherwise.","When TLS certificates are configured, TLS and HTTP/3 are implied on the listen port; http3_addr: :8443 pins the QUIC listener to another UDP port."]},{"i":"listeners-code-1","c":["listeners:\n  - \":8080\"            # plaintext; HTTP/1.1 + h2c\n  - \":443 ssl h2\"      # HTTPS; HTTP/1.1 + HTTP/2 via ALPN\n  - \":443 h3\"          # HTTP/3 (UDP) on the same port (implies TLS)\n  - \":8443 ssl h3\"     # HTTPS + HTTP/3 on a dedicated port (UDP + TCP)"]},{"i":"listeners-code-2","c":["listen: \":8081\""]},{"l":"TLS","p":["Certificates can come from three places (in increasing specificity):","Global tls_cert_file / tls_key_file in config.yaml.","Per-target tls: blocks (served via SNI).","Self-signed certificate generated automatically whenever TLS or HTTP/3 is requested but no valid certificate can be loaded.","Reloading (SIGHUP) re-applies global and per-target certificates; HTTP/3 listeners pick up the refreshed bundle immediately."]},{"l":"HTTP/3 (QUIC)","p":["HTTP/3 is served by default on the same port as the TLS frontend, and can be pinned to another UDP port:",":443 h3 alone serves HTTP/3 only (UDP); combine with :443 ssl (or one :443 ssl h3 line) to also serve HTTPS over TCP on the same port.","The frontend negotiates h3 automatically over QUIC; the same certificate bundle serves the TCP (h1/h2) and UDP (h3) listeners.","HTTP/3 requests share the host/location router and plugins, but live outside the disk cache path (no caching/HIT on HTTP/3 responses).","The QUIC listener is torn down during graceful shutdown and its TLS config refreshed on SIGHUP.","See HTTP/3 (QUIC) for the full details."]},{"l":"TLS certificate resolution","p":["TLS is resolved per connection via GetConfigForClient: SNI-based per-target certificates first, then the global certificate, then the generated self-signed certificate. Clients must trust the self-signed certificate explicitly when it is used (e.g. curl -k)."]}],[{"l":"Plugins","p":["Peretum loads plugins from a registry and dispatches lifecycle hooks around the request path. Plugins are configured globally in config.yaml (global settings) and per-location in config.d/*.yaml (their behavior)."]},{"l":"The registry","p":["compression","cors","enabled is the universal load gate — a plugin whose enabled key is false is not instantiated at all.","Error pages","error_page","headers","JSON logs","jsonlog","Location-scoped","Location-scoped plugins (cors, headers, rewrite, compression, optimizer) produce one global instance: buildPluginConfigs overwrites the config for every configured location, so the last location in load order wins and the resulting plugin applies to every request on every location. The WAF plugin deliberately avoids this by aggregating a target|location indexed map (see WAF).","Name","no","Notes:","optimizer","Page","per target+location","Prometheus metrics","prometheus_exporter","Registered plugins:","Reload (SIGHUP) re-applies TLS/router/log-rotation but does not re-run plugin Init; behavior changes require a restart.","rewrite","waf","yes"]},{"l":"Lifecycle hooks","p":["Request handling in internal/handler/handler.go dispatches:","BeforeProxy — before the upstream fetch (also before cache lookup): CORS, request headers, rewrite, WAF.","AfterProxy — after the upstream response: response headers.","ResponseBodyHook / TransformResponseBody — on the store-to-cache path only: optimizer.","Cache hooks — cache-hit/miss/eviction accounting for metrics.","RouterWrapper — error_page wraps the whole frontend router.","ErrorHook — jsonlog receives proxy/transform/cache errors.","gRPC requests (Content-Type application/grpc*) bypass caching, rewriting, CORS preflight handling, optimization, compression and response buffering; they still pass through BeforeProxy (so WAF applies) and the request logger."]},{"l":"Caveats","p":["CORS, headers and rewrite each have both the plugin hook path and a native per-location path in the handler. When the same location feeds both (which the wiring does), the transform is applied twice (idempotent header sets; rule rewrites and preflight short-circuits are one-shot)."]},{"l":"Pages","p":["Compression","Optimizer","CORS","Headers","Rewrite","JSON logs","Prometheus metrics","Error pages","Web Application Firewall"]}],[{"l":"Compression","p":["—","[]string","1024","5","bool","Default","Description","enabled","gzip level, 1–9.","int","Key","level","Location-scoped config (last location wins; applies proxy-wide):","Master switch.","MIME types treated as compressible, merged over the defaults.","min_length","Only compress bodies at least this large.","see below","Setting enable: true is required — a location whose compression block is disabled emits enabled: false and the plugin is not loaded.","Transparent gzip compression of upstream responses.","Type","types"]},{"i":"compression-code-1","c":["locations:\n  - path: \"/\"\n    compression:\n      enabled: true\n      level: 5             # 1-9 (default 5)\n      min_length: 1024     # minimum response body size to compress (default 1024)\n      types:               # extra MIME types to compress\n        - \"application/vnd.api+json\""]},{"l":"Built-in compressible types","p":["text/html, text/css, text/javascript, application/javascript, application/json, application/xml, text/xml, text/plain, application/wasm, image/svg+xml.","Configured types are normalized (lowercased, whitespace trimmed, ;charset stripped) and merged on top of these."]},{"l":"Behavior","p":["Compresses response bodies when the client sends Accept-Encoding: gzip (the upstream request has it stripped), the Content-Type is compressible, and the body is = min_length.","Sets Content-Encoding: gzip, Vary: Accept-Encoding, and removes Content-Length (the response is fully buffered before deciding).","Applies only on the reverse-proxied, non-gRPC path; cache hits and gRPC streams pass through untouched."]}],[{"l":"Optimizer","p":["—","0 (no resize)","85","bool","Declared output format (webp/avif/jpeg/png).","Default","Description","Enable image optimization.","enabled","false","format is not used at runtime. Images are re-encoded in their original decoded format (JPEG or PNG only) so the stored Content-Type stays valid; WebP and GIF pass through untouched. Undecodable images pass through unchanged.","images.enabled","images.format","images.max_width / max_height","images.progressive","images.quality","images.strip_metadata","int","JPEG re-encode quality, 1–100.","Key","Location-scoped config (last location wins; applies proxy-wide):","Master switch.","Minifies CSS/JavaScript and re-encodes/resizes JPEG and PNG images.","Minify application/javascript / text/javascript / *js.","Minify text/css / *css responses.","minify_css","minify_js","Progressive encoding.","Resize bounds (Catmull-Rom scaling).","Same minification path as minify_js.","string","Strip EXIF/metadata.","Type","uglify_js"]},{"i":"optimizer-code-1","c":["locations:\n  - path: \"/\"\n    optimize:\n      enabled: true\n      minify_css: true\n      minify_js: true\n      uglify_js: true\n      images:\n        enabled: true\n        max_width: 1600\n        max_height: 1200\n        quality: 82          # 1-100 (default 85)\n        format: \"webp\"       # see note below\n        strip_metadata: true\n        progressive: true"]},{"l":"Where it runs","p":["TransformResponseBody runs only on the caching path: after a successful 2xx upstream fetch, immediately before the entry is stored — so the cache entry is the optimized one and subsequent cache hits serve the transformed body. Non-cached locations, live (non-cached) first responses, and gRPC never see transformations."]}],[{"l":"CORS","p":["—","[]string","0","Access-Control-Expose-Headers.","Access-Control-Max-Age when 0.","allow_credentials","allow_headers","allow_methods","allow_origins","bool","Cross-Origin Resource Sharing response headers.","Default","Description","Emitted in Access-Control-Allow-Methods.","empty","enabled","expose_headers","false","GET, POST, PUT, DELETE, OPTIONS, HEAD","If empty, echoes Access-Control-Request-Headers.","int","Key","Location-scoped config (last location wins; applies proxy-wide):","Master switch.","max_age","Origin is allowed if an entry is * or matches exactly.","Sets Access-Control-Allow-Credentials: true.","Type"]},{"i":"cors-code-1","c":["locations:\n  - path: \"/\"\n    cors:\n      enabled: true\n      allow_origins:\n        - \"https://app.example.com\"\n        - \"https://dev.example.com\"\n      allow_methods: [\"GET\", \"POST\", \"PUT\", \"DELETE\"]\n      allow_headers: [\"Authorization\", \"Content-Type\", \"X-Request-ID\"]\n      expose_headers: [\"X-Total-Count\"]\n      allow_credentials: true\n      max_age: 3600"]},{"l":"Behavior","p":["Runs in BeforeProxy (before the cache lookup), so it also applies to cached responses.","Access-Control-Allow-Origin echoes the requesting origin — * is accepted in config but never emitted verbatim when credentials are used.","An OPTIONS request from an allowed origin short-circuits with 204 No Content and is never proxied or cached.","Note: the handler also runs a native per-location CORS path when location.CORS.Enabled is set, so the same headers are written twice (idempotent overwrites). The plugin instance matches every location once enabled."]}],[{"l":"Headers","p":["Adds and removes request and response headers.","Location-scoped config (last location wins; applies proxy-wide):","Key","Type","Description","request_add","map[string]string","Headers set on the upstream request.","request_remove","[]string","Headers removed from the upstream request.","response_add","Headers set on the client response.","response_remove","Headers removed from the client response."]},{"i":"headers-code-1","c":["locations:\n  - path: \"/\"\n    headers:\n      request_add:\n        X-Forwarded-By: \"peretum\"\n        X-API-Version: \"v2\"\n      request_remove:\n        - \"X-Internal-Token\"\n      response_add:\n        X-Powered-By: \"peretum\"\n        Cache-Control: \"public, max-age=300\"\n      response_remove:\n        - \"Server\""]},{"l":"Behavior","p":["request_* mutations apply in BeforeProxy (before the upstream fetch), including on cache-hit responses.","response_* mutations apply in AfterProxy, on the non-gRPC proxied path.","The plugin applies to every request once loaded.","Note: the handler also applies the location's headers block natively (applyRequestHeaders / applyResponseHeaders on both cache-hit and proxied paths), so header mutations can be applied twice when the plugin and the location config are both present — idempotent for add, additive for remove."]}],[{"l":"Rewrite","p":["= rewrite in place; redirect = 302; permanent = 301.","A configured redirect issues the matching status and short-circuits the request (never proxied or cached).","Applies to every request once loaded — including locations that did not declare a rewrite block.","Behavior:","bool","break","Description","Key","Location-scoped config (last location wins; applies proxy-wide):","Note: the handler also applies the location's rewrite block natively on the non-gRPC path, so a single location feeding both can apply the rewrite twice (the plugin runs globally).","pattern","redirect","Regex matched against r.URL.Path (required).","Regex-based request path rewrites, with optional $N capture groups, break and redirect support.","replacement","Replacement; supports $1, $2, … (required).","Rewrites r.URL.Path in BeforeProxy; the rewritten path is what gets proxied and cached.","Rules with an empty pattern/replacement or an un-compilable regex are skipped with a warning.","Stop after the first matching rule.","string","Type"]},{"i":"rewrite-code-1","c":["locations:\n  - path: \"/\"\n    rewrite:\n      pattern: \"^/api/v1/(.*)\"\n      replacement: \"/v2/$1\"\n      break: false\n      redirect: \"\"          # \"\", \"redirect\" (302), or \"permanent\" (301)"]}],[{"l":"JSON logs","p":["Access log file; - = stdout.","access_log","access.jsonl","Additionally mirror every line to stdout.","bool","Default","Description","enabled","Error log file; - = stdout.","error_log","error.jsonl","false","Global config in config.yaml:","Key","Master switch.","nginx-style access and error logs as JSON Lines (JSONL).","stdout","string","Type"]},{"i":"json-logs-code-1","c":["json_log:\n  enabled: true\n  access_log: \"./logs/access.jsonl\"\n  error_log: \"./logs/error.jsonl\"\n  stdout: true"]},{"l":"Access entries","p":["Recorded for every request (including cache hits and gRPC):","method, uri, host, remote_addr, protocol, target, location, user_agent, referer, request_id, level, type, time, status, bytes_sent, request_time, cache (HIT/MISS), content_type."]},{"l":"Error entries","p":["Routed from proxy/gRPC errors, transform failures and cache-write failures via the ErrorHook:","level, type: error, time, msg, target, location, request_id, plus arbitrary extra fields."]},{"l":"File handling","p":["Start opens buffered (64 KB) writers; lines are flushed per record.","Reopen() rotates the underlying files on SIGHUP — drop-in for logrotate and similar.","Stop flushes and closes the writers."]}],[{"l":"Prometheus metrics","p":["Exports an HTTP /metrics endpoint on a dedicated listener.","Enabled automatically when metrics_addr is set in config.yaml:","The plugin config is derived from the global settings:","Key","Source","Default","listen","metrics_addr",":9090"]},{"i":"prometheus-metrics-code-1","c":["metrics_addr: \":9090\""]},{"l":"Metrics","p":["counter","gauge","histogram","Metric","peretum_cache_entries","peretum_cache_evictions_total","peretum_cache_expirations_total","peretum_cache_hits_total","peretum_cache_misses_total","peretum_cache_size_bytes","peretum_location_cached_requests_total{location}","peretum_location_requests_total{location}","peretum_proxy_active_requests","peretum_proxy_cached_requests_total","peretum_proxy_request_duration_seconds","peretum_proxy_requests_total","peretum_target_cached_requests_total{target}","peretum_target_requests_total{target}","Type","Wiring note: the runtime request/cache path currently calls the cache-hit and cache-miss recorders. The remaining plugin hooks (RecordRequest, active-request gauge, histogram, eviction/expiration counters, cache size/entries) implement the full MetricsHook surface but are not yet invoked from the runtime path — they are exercised in tests."]}],[{"l":"Error pages","p":["Replaces default error responses with branded HTML pages.","Global config in config.yaml:","Key","Type","Default","Description","enabled","bool","false","Master switch.","statuses","[]int","[404, 500, 502, 503, 504]","Status codes rendered as branded pages."]},{"i":"error-pages-code-1","c":["error_page:\n  enabled: true\n  statuses: [404, 500, 502, 503, 504]   # default: [404, 500, 502, 503, 504]"]},{"l":"Behavior","p":["The plugin wraps the top-level router (RouterWrapper), so it intercepts:","router-level misses (e.g. 404 No matching target/location) that never reach a target handler;","every configured 4xx/5xx from any target — including WAF blocks (which write a text/plain body with X-WAF-Block: true).","For configured statuses it substitutes an escaped, branded HTML page (embedded logo, request ID, time, host, method, path, client address), forces Content-Type: text/html; charset=utf-8, removes Content-Length / Content-Encoding, sets X-Request-ID and Cache-Control: no-store, and suppresses the body for HEAD."]}],[{"l":"Web Application Firewall (WAF)","p":["The waf plugin inspects requests before proxying and blocks or audits them against per-location rule sets. It runs from BeforeProxy, so it applies to proxied, cached and gRPC requests alike.","Unlike the other location-scoped plugins (which collapse to a single global instance), the WAF aggregates every location that declares a waf: block into a target|location-indexed policy map, so each location gets exactly its own rules. Locations without a waf: block are not covered by the WAF."]},{"l":"Global settings (config.yaml)","p":["Key","Type","Description","enabled","bool","Master switch.","geolite_dir","string","Directory with GeoLite2-Country.mmdb (required), GeoLite2-ASN.mmdb (optional), GeoLite2-City.mmdb (optional). A missing Country DB is fatal; missing ASN/City DBs degrade gracefully (their params just return empty)."]},{"i":"global-settings-configyaml-code-1","c":["waf:\n  enabled: true\n  geolite_dir: \"./plugins/waf/geolite\""]},{"l":"Per-location policy (config.d/*.yaml)"},{"i":"per-location-policy-configdyaml-code-1","c":["locations:\n  - path: \"/\"\n    waf:\n      enabled: true\n      rules:\n        # Local IP bypass (replaces the former ip_whitelist gate key).\n        - id: \"allow-localhost\"\n          name: \"Always allow local development traffic\"\n          enabled: true\n          action:\n            type: \"allow\"\n          conditions:\n            - - param: \"ip\"\n                operator: \"in_ip\"\n                value: \"127.0.0.1, ::1\"\n        # Geo + IP enforcement — rule-based conditions replace geo gate keys.\n        - id: \"geo-ip\"\n          name: \"Geo + IP enforcement\"\n          enabled: true\n          action:\n            type: \"deny\"\n            code: 403\n            message: \"Geo/IP rule blocked\"\n          conditions:\n            - - param: \"country\"\n                operator: \"contains\"    # case-insensitive substring: \"ir\" matches IR\n                value: \"ir\"\n            - - param: \"asn\"\n                operator: \"in\"\n                value: \"4134,4837\"\n            - - param: \"city\"\n                operator: \"equals\"\n                value: \"Tehran\"\n            - - param: \"ip\"\n                operator: \"in_ip\"\n                value: \"8.8.8.0/24, 51.0.0.0/8\"\n        # SQL injection\n        - id: \"sql-select\"\n          name: \"Block obvious SQL injection\"\n          enabled: true        # per-rule toggle\n          action:\n            type: \"deny\"       # deny | allow | log\n            code: 403          # HTTP status for the block page\n            message: \"SQL injection attempt blocked\"   # block page body\n          conditions:\n            # OR between groups, AND within a group (DNF, mirrors the Lua WAF).\n            - - param: \"body\"\n                operator: \"contains\"\n                value: \"select\"\n              - param: \"body\"\n                operator: \"contains\"\n                value: \"from\"\n            - - param: \"query\"\n                operator: \"matches\"\n                value: \"union.*select\""]},{"l":"Evaluation order","p":["Rules are evaluated in the order they appear in the rules: list. A rule with action.type: allow short-circuits the evaluation (the request passes immediately). The first matching deny rule blocks the request. A rule with action.type: log records the match and evaluation continues.","A rule matches when any group of conditions matches (DNF). Geo and IP checks (country, asn, asn_org, city, ip) are ordinary condition parameters and work the same way as header or body checks."]},{"l":"Conditions","p":["A rule matches when any group of conditions matches; within a group, all conditions must match (DNF). Operators are parsed case-insensitively:","client IP is/is-not contained in the comma-separated IP/CIDR value list","contains","endswith","equals","exact string equality (for the ip param: network containment)","exists / not_exists","For the ip parameter, equals, in, not_in, in_ip and not_in_ip perform network containment: value is a comma-separated list of plain IPs (treated as /32 or /128) and/or CIDR blocks, and the condition fires when the effective client IP is (or is not) inside any of them.","gt / lt","in / not_in","in_ip / not_in_ip","matches","Matches when","numeric comparison (asn, score-like numeric params)","Operator","param is present / absent","param value contains value (Aho–Corasick automaton)","regexp value matches","startswith","value ends with value","value is/is-not in the comma-separated value list (for the ip param: member of any listed IP/CIDR)","value starts with value"]},{"l":"Parameters","p":["arg","AS number","AS organization","asn","asn_org","body","city","city name (en locale of GeoLite2 City)","Client addresses and network blocks are matched as part of a rule too:","cookie","Cookie header","country","country, asn, asn_org and city are ordinary condition parameters. Use equals / in / not_in for exact geo matches and gt / lt for numeric ASN ranges:","effective client IP (network-containment checks)","full request URI","header","host","Host header","HTTP method","ip","ISO country code from GeoLite Country","method","Param","path","path only","query","query argument named by param_name","raw query string","referer","Referer header","request body","request header named by param_name","url","user_agent","User-Agent header","Value"]},{"i":"parameters-code-1","c":["- param: \"country\"\n  operator: \"in\"\n  value: \"IR,CN,RU\"\n- param: \"asn\"\n  operator: \"gt\"\n  value: \"30000\"\n- param: \"city\"\n  operator: \"equals\"\n  value: \"Tehran\""]},{"i":"parameters-code-2","c":["- param: \"ip\"\n  operator: \"not_in\"          # allow every cloud block except these\n  value: \"1.2.3.0/24, 192.168.0.0/16, 10.10.10.10\"\n- param: \"ip\"\n  operator: \"in_ip\"\n  value: \"8.8.8.0/24\""]},{"l":"Performance","p":["All contains patterns for a parameter are merged into a single Aho–Corasick automaton, so each request field is scanned once regardless of how many rules reference it."]},{"l":"Client IP resolution","p":["X-Real-IP > first hop of X-Forwarded-For > RemoteAddr."]},{"l":"Blocking","p":["Blocks write a plain-text body with X-WAF-Block: true and the status from the matched rule's action.code, default 403. A rule without action.code or action.message uses 403 and Request blocked by WAF. The error page plugin layers its branded page on top."]}],[{"l":"HTTP/3 (QUIC)","p":["HTTP/3 is served by default on the same port as the TLS frontend whenever a TLS certificate is active, and can be pinned to another UDP port with http3_addr. With the listeners: form, per-listener control is explicit."]},{"l":"Configuration","p":["Form","Effect","listeners: [:443 ssl h3]","HTTPS over TCP and HTTP/3 over UDP on :443.","listeners: [:443 h3]","HTTP/3 only (UDP socket on :443).","listeners: [:443 ssl, :443 h3]","HTTPS (TCP) + HTTP/3 (UDP) on the same port.","legacy listen + http3_addr","TLS implied on listen port; QUIC pinned to http3_addr.","In the legacy shorthand, when TLS is active and http3_addr is unset, the QUIC listener runs on the listen address (same port, UDP); set http3_addr: :8443 to use a dedicated UDP port."]},{"l":"TLS requirement","p":["HTTP/3 implies TLS. TLS requires either global tls_cert_file/tls_key_file or per-target tls: blocks. If TLS or HTTP/3 is requested but no valid certificate can be loaded, the proxy generates a self-signed certificate automatically (clients must trust it explicitly, e.g. curl --http3 -k), so HTTP/1.1, HTTP/2, and HTTP/3 all work out of the box."]},{"l":"Runtime behavior","p":["ALPN — the frontend negotiates h3 automatically over QUIC; the same certificate bundle serves both the TCP (h1/h2) and UDP (h3) listeners.","Routing & plugins — HTTP/3 requests share the same host/location router and plugin stack (including the WAF).","Caching — HTTP/3 responses live outside the on-disk cache path; no caching or X-Cache: HIT on HTTP/3.","Lifecycle — the QUIC listener is torn down during graceful shutdown (SIGINT/SIGTERM) and its TLS config is refreshed on SIGHUP reload."]},{"l":"Verification","p":["The suite also round-trips a quic-go HTTP/3 client through the proxy (proto=HTTP/3.0)."]},{"i":"verification-code-1","c":["curl --http3 -k https://localhost:8443/     # needs a curl build with HTTP/3"]}],[{"l":"gRPC proxying","p":["gRPC requests are HTTP/2 streams. Peretum supports them as tunneled pass-through: end-to-end transport (HTTP/2) and binary bodies are left untouched, and no gRPC-specific features (messages, trailers, reflection) are interpreted."]},{"l":"How it works","p":["gRPC traffic is identified by Content-Type: application/grpc (or application/grpc+proto etc.).","Hooks are bypassed for gRPC requests: no caching, no rewriting, no CORS preflight handling, no optimization, no compression and no response buffering.","BeforeProxy still runs, so the WAF and request-header rewrite apply to gRPC requests too (see Plugins)."]},{"l":"Server requirements","p":["The upstream must accept HTTP/2:","h2c — for cleartext (listen: :8080 or listeners: [:8080 h2c]);","ALPN h2 — for TLS (listeners: [:443 ssl h2]).","Bi-directional streaming is supported (the proxy never buffers the request or response body): client streams, server streams and bidi all pass through."]},{"l":"Configuration","p":["Provide http2-aware frontends via listeners::","The default config.yaml listens on :8080 (h2c), :443 (TLS + HTTP/3) and :8443 (TLS + HTTP/3), so both cleartext and TLS gRPC clients work out of the box."]},{"i":"configuration-code-1","c":["listeners:\n  - \":8080 h2c\"      # cleartext gRPC (no TLS)\n  - \":443 ssl h2\"    # TLS gRPC with ALPN"]},{"l":"Manual check"},{"i":"manual-check-code-1","c":["grpcurl -plaintext -d '{}' localhost:8080 helloworld.Greeter/SayHello\ngrpcurl -d '{}' localhost:443 helloworld.Greeter/SayHello"]}],[{"l":"Disk cache","p":["Peretum stores backend responses as files on disk and serves subsequent requests straight from disk — no network, no upstream connection. The cache is transparent: the proxy only caches responses that are cacheable, keyed by method + route + upstream + request body hash when applicable."]},{"l":"Cache keys","p":["A response is cached per (target, route, method + path + query) and per cache_ttl. POST/PUT/PATCH requests include a body hash in the key (same request body reuses a cached response). A response is served from cache only if the GET/HEAD/POST request's body hash matches (when required)."]},{"l":"Cacheable responses","p":["Responses must have:","an HTTP status in the 200 range (200–299);","no Set-Cookie header;","no Authorization header in the request (varies)."]},{"l":"Configuration","p":["—","./cache","0","0 (unlimited)","cache","cache_dir","cache_ttl","Default","Description","Enable caching for the location.","Entry lifetime via time.ParseDuration.","false","global","global (config.yaml)","Key","location","Max total bytes; LRU-evicted.","max_cache_age","max_cache_size","Overrides max_cache_age per location.","Root directory (created on demand).","Where"]},{"l":"Transparent headers","p":["X-Cache: HIT — response served from the disk cache.","X-Cache: MISS — response fetched from upstream and cached (when cacheable)."]},{"l":"Strict verification","p":["curl -sI returns X-Cache: HIT only on truly cached responses; the cache is verified to stall/expire at the exact configured cache_ttl (17 s) and to serve HIT immediately after a MISS."]},{"l":"HTTP/3 note","p":["HTTP/3 (QUIC) responses bypass the disk cache path entirely (see HTTP/3 (QUIC))."]}],[{"l":"Routing","p":["Peretum routes requests in two steps: host routing selects a target, and location routing selects the backend within that target."]},{"l":"Host routing","p":["The router holds a map of host keys → targets. Hosts come from:","a target's listen host part (e.g. api.example.com);","a target's name — used as the host key when listen has no host;","the legacy server_name shorthand.","Requests are matched by the Host header (lowercased, port stripped).","Targets with no host key act as the default/fallback handler; the router also falls back to a target whose first matching location is path: /."]},{"l":"Location routing","p":["Inside a target, the first location whose path matches (ordered by priority, not definition order) wins:","match_type","Priority","Notes","exact","1000","path == request path","regex","500","full Go regex against the path","prefix (default)","length of trimmed path","longest prefix wins; / matches everything"]},{"i":"location-routing-code-1","c":["locations:\n  - path: \"/api/\"\n    match_type: \"prefix\"\n    upstream: \"http://localhost:8082\""]},{"l":"Load balancing","p":["An LB algorithm picks an upstream from the target's upstreams list:","Value","Behavior","round_robin / rr / (empty)","Equal distribution.","weighted_rr / wrr","Respects weight (smooth).","maglev","consistent hashing over 65537 slots, keyed by CRC32(path + rawQuery).","least_conn","upstream with the fewest active requests.","All algorithms skip unhealthy upstreams; a whole-target failure returns 503, and the LB pool is repaired by the health tracker (see Targets)."]},{"l":"gRPC routing","p":["gRPC streams use the same host/location router, so grpc:// backends and mixed HTTP/gRPC targets share one route table."]}],[{"l":"Development","p":["Everything you need to extend Peretum: repo layout, the testing bar, and how to add new plugins and targets.","Project layout","Testing","Adding a plugin","Contributing"]},{"l":"Quick dev loop","p":["Tests bind fixed ports (:8081, :8082, :9090) — stop any proxy / testserver / metrics process on those before running the suite, or port-conflict tests skip."]},{"i":"quick-dev-loop-code-1","c":["go build ./...                                # compile everything\ngo run .                                      # run the proxy against config.yaml/config.d/\ngo run ./cmd/testserver                       # upstream test server on :8082"]}],[{"l":"Project layout"},{"i":"project-layout-code-1","c":["peretum/\n├── config.yaml              # Global proxy config\n├── config.d/                # Target configs (one per file)\n│   ├── api.yaml\n│   ├── fallback.yaml\n│   ├── static.yaml\n│   └── test.yaml\n├── main.go                  # Entrypoint: delegates to cmd.Execute()\n├── cmd/                     # cobra-cli style command tree\n│   ├── root.go              # Root command + flags (-t/-r, --config, ...)\n│   ├── run.go               # Default run (pid file + server lifecycle)\n│   ├── reload.go            # --reload: SIGHUP to the pid in --pid-file\n│   ├── check.go             # --test: config syntax check without starting\n│   └── server.go            # Proxy server assembly (routers, TLS, HTTP/3)\n├── cmd/testserver/          # Upstream test server\n├── docs/                    # GitHub Pages documentation (this site)\n├── internal/\n│   ├── cache/disk/          # Disk cache (shards, atomic writes, eviction)\n│   ├── config/              # YAML loading + parsing\n│   ├── handler/             # Proxy handler, caching, coalescing\n│   ├── loadbalancer/        # LB algorithms\n│   ├── plugin/manager/      # Plugin registry hook dispatch\n│   └── router/              # Host + location routing\n└── plugins/\n    ├── base/                # Plugin interface + helpers\n    ├── compression/\n    ├── cors/\n    ├── errorpage/           # Branded HTML error pages\n    ├── headers/\n    ├── jsonlog/             # JSON access/error logs\n    ├── optimizer/           # CSS/JS minification + image optimization\n    ├── prometheus/          # Metrics exporter\n    ├── registry/            # Plugin registration/lifecycle\n    ├── rewrite/\n    └── waf/                 # Web Application Firewall (rules + geo filters)"]},{"l":"Key entry points","p":["CLI and flags","cmd/root.go","cmd/server.go","Concern","Config types and parsers","Host + location routing","internal/config/config.go","internal/handler/handler.go","internal/loadbalancer/loadbalancer.go","internal/plugin/manager/manager.go","internal/router/router.go","LB algorithms","Location","Plugin contracts","Plugin hook implementation","Plugin registration","plugins/base/base.go","plugins/registry/init.go","Request lifecycle and plugin hook dispatch","Server assembly, plugin wiring (buildPluginConfigs)"]}],[{"l":"Testing","p":["Every package must keep 100% statement coverage — new code requires matching tests, never a relaxed bar."]},{"i":"testing-code-1","c":["go test ./... -cover           # suite must stay at 100% per package\ngo test ./internal/cache/disk/ -v -race\ngo test ./internal/cache/disk/ -bench=."]},{"l":"Gates to run before submitting"},{"i":"gates-to-run-before-submitting-code-1","c":["go build ./...                 # compile everything\ngo vet ./...                   # static checks\ngofmt -l .                     # nothing printed\ngo test ./... -race            # full suite under the race detector"]},{"l":"Coverage","p":["All 20 packages (including the cmd command tree and the main entrypoint) carry suite-level tests at 100% statement coverage. Check per-package coverage with:"]},{"i":"coverage-code-1","c":["go test -cover ./cmd/ ./internal/config/ ./plugins/waf/ ..."]},{"l":"Ports","p":["Tests bind fixed ports — :8081, :8082, :9090. Stop any proxy, testserver or metrics process on those ports before running the suite, or port-conflict-dependent tests will be skipped."]},{"l":"Test fixtures","p":["The WAF package generates its own GeoLite-style Country/ASN/City .mmdb fixtures in-tests (mmdbwriter) so geo lookups run without downloading MaxMind databases.","TLS tests generate in-memory certificates (crypto/x509)."]}],[{"l":"Adding a plugin","p":["Implement the contracts from plugins/base/base.go:","Plugin — Name(), Init(config map[string]any), Start(ctx), Stop(ctx) (embed base.BasePlugin for the shared no-op defaults).","Optional interfaces discovered structurally:","RequestHook — BeforeProxy, AfterProxy","ResponseBodyHook — TransformResponseBody","CacheHook — BeforeCacheStore, AfterCacheHit, ShouldCache","MetricsHook — RecordRequest, RecordCacheHit/Miss/Eviction/Expiration","ErrorHook — LogError","RouterWrapper — WrapRouter (frontend-level wrapping)","Reopen — log-rotation on SIGHUP","Register it in plugins/registry/init.go:","Wire the config in cmd/server.go buildPluginConfigs (and, for location-scoped plugins, a structToMap-style conversion) so that your plugin's keys are emitted with enabled as the load gate.","Enable it under the plugins:/location settings in your YAML config."],"c":["Register(\"my_plugin\", func() base.Plugin { return myplugin.New() })"]},{"l":"Data model","p":["Init receives a map[string]any; use the panic-free accessors (base.GetBool, GetInt, GetString, GetStringSlice) from plugins/base/helpers.go.","Request IDs are available via base.EnsureRequestID / base.RequestID.","Location-scoped plugins are global and last-wins today; if your plugin needs per-location policies (like the WAF), aggregate a target|location-indexed map in the wiring instead."]},{"l":"Tests","p":["New code requires the package to stay at 100% statement coverage — write tests for Init, Start/Stop, and every hook you implement, and run the full gate set before submitting."]}],[{"l":"Contributing"},{"l":"Adding a target","p":["Drop a new YAML file in config.d/ (see the targets reference), verify it with peretum -t, then SIGHUP a running proxy (or use peretum -r, or restart it). No code changes needed."]},{"l":"Changing behavior","p":["Document any config/behavior change in this documentation site (and the README.md), and keep the test coverage at 100% per package."]},{"l":"Documentation site","p":["This site is built from docs/ by Retype and published to GitHub Pages:","Source: docs/ — plain Markdown with label/order front matter.","Project config: docs/retype.yml (title, links, edit-on-GitHub, last-updated footer).","Navigation is generated automatically: files in a folder become children of that folder's index page; order (larger = higher) sorts the sidebar, icon adds an Octicon.","Home page: docs/index.md.","Links: internal links reference .md paths (e.g. ../configuration/targets.md) and are resolved to the generated pages automatically.","Deployment: .github/workflows/retype-action.yml builds with retypeapp/action-build and publishes with retypeapp/action-github-pages on every push to main.","Preview locally:","Build the static site without a server:"]},{"i":"documentation-site-code-1","c":["npx retypeapp start          # serves docs/ with live reload\n# http://localhost:5000/"]},{"i":"documentation-site-code-2","c":["npx retypeapp build          # writes the site to docs/.retype"]},{"l":"PR checklist","p":["go build ./... and go vet ./... are clean","gofmt -l . prints nothing","go test ./... -race passes and go test ./... -cover stays at 100% statement coverage in every package","Config/behavior changes are documented in this docs site and the README"]}]]